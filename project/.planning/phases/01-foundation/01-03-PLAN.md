---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [aicomp/src/app/layout.tsx, aicomp/src/app/page.tsx, aicomp/src/components/layout/sidebar.tsx, aicomp/src/components/layout/header.tsx]
autonomous: false
---

<objective>
Build the main application layout with glass sidebar navigation and dashboard shell, then verify the visual design.

Purpose: Create the structural foundation for the AI intelligence hub. The layout establishes the navigation pattern and dashboard grid that all feature pages will use.

Output: Working application shell with glass sidebar, header, and dashboard placeholder content that demonstrates the Opta hybrid aesthetic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create glass sidebar navigation component</name>
  <files>aicomp/src/components/layout/sidebar.tsx</files>
  <action>
  Create a fixed glass sidebar for main navigation:

  ```
  - Width: 240px on desktop, collapses on mobile
  - Glass effect: bg-glass-bg backdrop-blur-md border-r border-glass-border
  - Logo/brand at top: "AI Comp" or site name
  - Navigation links (placeholder for now):
    - Dashboard (home icon)
    - Models (cpu icon)
    - Benchmarks (bar-chart icon)
    - Pricing (dollar-sign icon)
    - News (newspaper icon)
  - Use Lucide icons
  - Active state: neon-cyan accent
  ```

  Make it a client component ('use client') for mobile toggle state.

  Use Link from next/link for navigation items.

  AVOID: Complex state management. Keep it simple with useState for mobile toggle.
  </action>
  <verify>Component imports without errors, uses Lucide icons</verify>
  <done>Glass sidebar with navigation links and mobile-responsive toggle</done>
</task>

<task type="auto">
  <name>Task 2: Create header component with search placeholder</name>
  <files>aicomp/src/components/layout/header.tsx</files>
  <action>
  Create a header bar for the main content area:

  ```
  - Sticky at top of content area (not sidebar)
  - Glass effect: bg-glass-bg/50 backdrop-blur-sm
  - Left: Page title (dynamic, passed as prop)
  - Center: Search input (placeholder, non-functional)
  - Right: Theme toggle placeholder (sun/moon icon button)
  ```

  Search input styling:
  - bg-opta-surface border border-opta-border rounded-lg
  - Placeholder: "Search models, benchmarks..."
  - Search icon (lucide) inside

  AVOID: Implementing actual search functionality. This is UI scaffolding only.
  </action>
  <verify>Component exports cleanly, header has search input and title prop</verify>
  <done>Header with search placeholder and page title support</done>
</task>

<task type="auto">
  <name>Task 3: Update root layout with sidebar integration</name>
  <files>aicomp/src/app/layout.tsx</files>
  <action>
  Update the root layout to integrate sidebar:

  ```tsx
  // Layout structure:
  <html>
    <body>
      <div className="flex min-h-screen">
        <Sidebar />
        <main className="flex-1 ml-0 md:ml-60">
          <Header title="Dashboard" />
          <div className="p-6">
            {children}
          </div>
        </main>
      </div>
    </body>
  </html>
  ```

  Add Inter font from next/font/google for --font-sans.

  Ensure:
  - Sidebar is fixed position
  - Main content has proper margin on desktop
  - Mobile: sidebar overlays or slides in
  - Dark theme by default (html className="dark" or body bg-opta-bg)

  AVOID: Adding providers or complex wrappers. Keep layout minimal.
  </action>
  <verify>cd aicomp && npm run build succeeds; layout renders sidebar + main area</verify>
  <done>Root layout integrates sidebar with responsive behavior</done>
</task>

<task type="auto">
  <name>Task 4: Create dashboard shell page with sample content</name>
  <files>aicomp/src/app/page.tsx</files>
  <action>
  Create the dashboard home page with placeholder content showcasing components:

  ```
  Dashboard grid layout:
  - CSS Grid: 2 columns on desktop, 1 on mobile
  - Gap between cards

  Sample cards demonstrating design system:
  1. "Top Models" glass Card with Badge components
  2. "Recent Benchmarks" Card with placeholder chart area
  3. "Latest News" Card with list items
  4. "Quick Stats" Card with stat numbers

  Each card should:
  - Use Card component from ui/card
  - Glass variant for 1-2 cards, default for others
  - Include CardHeader with title
  - Placeholder content (lorem ipsum or "Coming soon")
  ```

  This demonstrates:
  - Card component usage
  - Badge component (in model cards)
  - Glass effects in context
  - Responsive grid

  AVOID: Real data or API calls. This is pure UI scaffolding.
  </action>
  <verify>cd aicomp && npm run build succeeds; page renders cards in grid</verify>
  <done>Dashboard page with sample cards demonstrating design system</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete application shell with glass sidebar, header, and dashboard with sample cards demonstrating the Opta hybrid design (dark theme + neon accents + glass effects)</what-built>
  <how-to-verify>
    1. Run: cd aicomp && npm run dev
    2. Visit: http://localhost:3000
    3. Check dark theme: Background should be dark (opta-bg)
    4. Check sidebar: Glass effect with navigation items, neon-cyan active state
    5. Check header: Search input visible, glass blur effect
    6. Check cards: Mix of glass and solid variants, proper spacing
    7. Desktop (>768px): Sidebar visible, 2-column grid
    8. Mobile (<768px): Sidebar collapses/hidden, single column
    9. Check: No layout shift, smooth appearance
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd aicomp && npm run build` succeeds
- [ ] `cd aicomp && npm run dev` shows working application
- [ ] Sidebar renders with glass effect and navigation
- [ ] Header renders with search input
- [ ] Dashboard shows card grid with mixed variants
- [ ] Responsive: layout adapts to mobile/desktop
- [ ] Human verification approved
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Visual design matches Opta hybrid aesthetic
- Human approved the visual result
- Foundation ready for Phase 2 (Data Pipeline)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
