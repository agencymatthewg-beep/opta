---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/App.tsx, src/App.css, src/components/Sidebar.tsx, src/components/Layout.tsx, src/pages/Dashboard.tsx, src/pages/Optimize.tsx, src/pages/Settings.tsx, src/index.css]
autonomous: true
---

<objective>
Create the main application UI shell with sidebar navigation and placeholder pages.

Purpose: Establish the visual structure and navigation patterns that all future features will use.
Output: Gaming-aesthetic dark theme UI with sidebar navigation between Dashboard, Optimize, and Settings pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app layout with sidebar navigation component</name>
  <files>src/components/Layout.tsx, src/components/Sidebar.tsx, src/App.tsx</files>
  <action>
    1. Create src/components/ directory
    2. Create Sidebar.tsx:
       - Fixed left sidebar (width: 240px)
       - Logo/app name at top ("Opta" with subtle glow effect)
       - Navigation items: Dashboard, Optimize, Settings
       - Use icons (can use simple SVG or emoji initially)
       - Active state highlighting
       - Gaming aesthetic: dark background (#0f0f0f), accent color (#00ff88 - neon green)

    3. Create Layout.tsx:
       - Flex container with sidebar + main content area
       - Main content takes remaining width
       - Proper padding and spacing

    4. Update App.tsx:
       - Import and use Layout component
       - Set up basic routing with useState for active page
       - No need for react-router yet - simple conditional rendering

    Styling approach:
    - Use CSS modules or inline styles (avoid adding Tailwind complexity in foundation)
    - Keep styles in component files or create matching .css files
    - Gaming aesthetic: dark, sleek, subtle glow effects, neon accents
  </action>
  <verify>
    - Sidebar renders on left side of window
    - Navigation items are clickable
    - Active item is visually distinguished
  </verify>
  <done>Sidebar navigation component working with gaming aesthetic dark theme</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder pages for main features</name>
  <files>src/pages/Dashboard.tsx, src/pages/Optimize.tsx, src/pages/Settings.tsx</files>
  <action>
    1. Create src/pages/ directory

    2. Create Dashboard.tsx:
       - Page title "Dashboard"
       - Placeholder cards for future widgets:
         - "System Status" (will show CPU/GPU/RAM)
         - "Optimization Score" (will show score)
         - "Active Optimizations" (will list applied tweaks)
       - Cards should have subtle borders and hover effects

    3. Create Optimize.tsx:
       - Page title "Optimize"
       - Placeholder for game list
       - Placeholder for "One-Click Optimize" button
       - Note: This is where the core value prop will live

    4. Create Settings.tsx:
       - Page title "Settings"
       - Placeholder sections:
         - "AI Configuration" (local vs cloud)
         - "Appearance"
         - "About"

    Wire up pages to sidebar navigation in App.tsx.

    Card styling:
    - Background: slightly lighter than page (#1a1a1a)
    - Border: subtle (#333)
    - Hover: slight glow or border highlight
  </action>
  <verify>
    - All three pages render when selected
    - Navigation switches between pages correctly
    - Placeholder content visible on each page
  </verify>
  <done>Three main pages created with placeholder content, navigation working</done>
</task>

<task type="auto">
  <name>Task 3: Implement dark theme and gaming aesthetic styling</name>
  <files>src/index.css, src/App.css</files>
  <action>
    1. Update src/index.css with global styles:
       - Reset defaults (margin, padding, box-sizing)
       - Body background: #0a0a0a (near black)
       - Default text color: #e0e0e0 (light gray)
       - Font: system-ui or Inter if available
       - Smooth scrolling

    2. Define CSS variables for theming:
       - --bg-primary: #0a0a0a
       - --bg-secondary: #0f0f0f
       - --bg-card: #1a1a1a
       - --text-primary: #ffffff
       - --text-secondary: #a0a0a0
       - --accent: #00ff88 (neon green)
       - --accent-glow: rgba(0, 255, 136, 0.3)
       - --border: #333333

    3. Add subtle animations:
       - Hover transitions (0.2s ease)
       - Focus states for accessibility
       - Subtle glow on accent elements

    4. Clean up default Tauri/Vite styles that don't fit the aesthetic

    The goal is a professional gaming tool aesthetic - think Discord meets NVIDIA GeForce Experience, not gaudy RGB rainbow.
  </action>
  <verify>
    - App has consistent dark theme
    - Accent color (#00ff88) used for interactive elements
    - No jarring white backgrounds or default browser styles
    - Hover states work smoothly
  </verify>
  <done>Gaming-aesthetic dark theme applied consistently across all components</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Sidebar navigation works (click items, pages switch)
- [ ] All three pages render with placeholder content
- [ ] Dark theme consistent throughout
- [ ] No visual bugs or style conflicts
- [ ] `npm run tauri dev` still works
</verification>

<success_criteria>
- All tasks completed
- UI shell established with working navigation
- Gaming aesthetic dark theme applied
- Placeholder pages ready for future feature implementation
- Foundation ready for Phase 2 (Hardware Telemetry UI)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
