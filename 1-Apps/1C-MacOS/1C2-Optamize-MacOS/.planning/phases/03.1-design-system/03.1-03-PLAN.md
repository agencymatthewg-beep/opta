---
phase: 03.1-design-system
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified: [src/components/TelemetryCard.tsx, src/components/TelemetryCard.css, src/components/CpuMeter.tsx, src/components/CpuMeter.css, src/components/MemoryMeter.tsx, src/components/MemoryMeter.css, src/components/GpuMeter.tsx, src/components/GpuMeter.css, src/components/DiskMeter.tsx, src/components/DiskMeter.css, src/components/ProcessList.tsx, src/components/ProcessList.css, src/components/StealthMode.tsx, src/components/StealthMode.css]
autonomous: true
---

<objective>
Restyle telemetry meters, process list, and Stealth Mode using shadcn/ui and Tailwind.

Purpose: Transform data visualization components to match the futuristic design system.
Output: Polished, cohesive telemetry and process management UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan (design system setup):
@.planning/phases/03.1-design-system/03.1-01-PLAN.md

Source files to restyle:
@src/components/TelemetryCard.tsx
@src/components/CpuMeter.tsx
@src/components/ProcessList.tsx
@src/components/StealthMode.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle TelemetryCard and meter components</name>
  <files>src/components/TelemetryCard.tsx, src/components/TelemetryCard.css, src/components/CpuMeter.tsx, src/components/CpuMeter.css, src/components/MemoryMeter.tsx, src/components/MemoryMeter.css, src/components/GpuMeter.tsx, src/components/GpuMeter.css, src/components/DiskMeter.tsx, src/components/DiskMeter.css</files>
  <action>
1. TelemetryCard.tsx:
   - Use shadcn Card component as base
   - Dark card background with subtle border
   - Hover state with glow effect (glow-sm on hover)
   - Title in muted color, value prominent

2. CpuMeter.tsx and GpuMeter.tsx (circular meters):
   - Keep SVG ring approach - it works well
   - Update colors to use CSS variables (--primary, --success, --warning, --danger)
   - Thresholds: <60% green, 60-85% amber, >85% red
   - Center value with large, bold text
   - Subtle glow on the ring based on value

3. MemoryMeter.tsx and DiskMeter.tsx (bar meters):
   - Use shadcn Progress component as base OR custom Tailwind bar
   - Gradient fill from primary to accent
   - Value label showing used/total
   - Clean, minimal design

4. Delete CSS files - move all to Tailwind:
   - TelemetryCard.css, CpuMeter.css, MemoryMeter.css, GpuMeter.css, DiskMeter.css

Color coding consistency:
- Green (#00ff88): Good/low usage
- Amber (#f59e0b): Warning/moderate
- Red (#ef4444): Critical/high
  </action>
  <verify>npm run dev (telemetry section shows styled meters)</verify>
  <done>All telemetry components restyled with consistent futuristic theme</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ProcessList component</name>
  <files>src/components/ProcessList.tsx, src/components/ProcessList.css</files>
  <action>
1. Rewrite ProcessList.tsx:
   - Use shadcn Card for container
   - Use shadcn Table components (Table, TableHeader, TableBody, TableRow, TableCell)
   - Use shadcn ScrollArea for scrollable content
   - Use shadcn Badge for category labels:
     - "system" → variant="secondary" (gray)
     - "user" → variant="default" (cyan/primary)
     - "safe-to-kill" → variant="outline" with amber text

2. Table styling:
   - Sticky header with dark background
   - Alternating row backgrounds (subtle)
   - Hover state on rows
   - Compact but readable
   - Columns: Name, CPU%, Mem%, Category, PID

3. Selection state (for future Stealth Mode integration):
   - Selected rows with primary/10 background
   - Checkbox or highlight indicator

4. Delete ProcessList.css - all Tailwind now

Keep the table scannable - users need to quickly identify resource hogs.
  </action>
  <verify>npm run dev (process list renders with new table styling)</verify>
  <done>ProcessList restyled with shadcn Table and Badge components</done>
</task>

<task type="auto">
  <name>Task 3: Restyle StealthMode component</name>
  <files>src/components/StealthMode.tsx, src/components/StealthMode.css</files>
  <action>
1. Rewrite StealthMode.tsx:
   - Hero button using shadcn Button with custom styling
   - Large, prominent "STEALTH MODE" button
   - Glow effect (glow-success) on hover
   - Pulsing animation when available processes exist

2. Confirmation dialog:
   - Use shadcn Dialog component
   - Dark overlay with blur
   - Clear title: "Activate Stealth Mode?"
   - List of processes to be terminated (use ScrollArea if many)
   - Estimated memory to be freed
   - Cancel (variant="outline") and Activate (variant="default" with green styling) buttons

3. Results display:
   - Use shadcn Card for results panel
   - Success state: green glow, checkmark icon
   - Show: X processes terminated, Y MB freed
   - Auto-dismiss with fade animation

4. Delete StealthMode.css - all Tailwind now

The button should feel POWERFUL - this is the hero action of the app.
Make it satisfying to click, with clear feedback.
  </action>
  <verify>npm run dev (Stealth Mode button and dialog work correctly)</verify>
  <done>StealthMode restyled as premium hero action with shadcn Dialog</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All telemetry meters display correctly with new colors
- [ ] ProcessList table is readable and styled consistently
- [ ] Category badges show correct colors
- [ ] StealthMode button is prominent and has glow effect
- [ ] Confirmation dialog opens and closes properly
- [ ] Results display after Stealth Mode activation
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- All data components feel cohesive with the design system
- StealthMode feels like the premium feature it is
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system/03.1-03-SUMMARY.md`
</output>
