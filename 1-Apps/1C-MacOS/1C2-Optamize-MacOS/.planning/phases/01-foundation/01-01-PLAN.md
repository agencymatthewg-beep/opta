---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/tauri.conf.json, src/main.tsx, src/App.tsx, tsconfig.json, vite.config.ts]
autonomous: true
---

<objective>
Initialize a Tauri v2 project with React/TypeScript frontend and Rust backend.

Purpose: Establish the foundational project structure that all future phases build upon.
Output: Working Tauri development environment with hot-reload, basic app window, and verified cross-platform compatibility.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tauri v2 project with React template</name>
  <files>package.json, src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/tauri.conf.json, src/, vite.config.ts, tsconfig.json</files>
  <action>
    Use `npm create tauri-app@latest` with these options:
    - Project name: opta
    - Package manager: npm
    - UI template: React
    - UI flavor: TypeScript

    After scaffolding:
    1. Update package.json with proper name "opta" and description
    2. Update src-tauri/tauri.conf.json:
       - productName: "Opta"
       - identifier: "com.opta.app"
       - title: "Opta - AI PC Optimizer"
       - width: 1200, height: 800 (larger default for dashboard)
    3. Update src-tauri/Cargo.toml package name to "opta"

    IMPORTANT: Use Tauri v2 (not v1). The create command should scaffold v2 by default in 2026.
  </action>
  <verify>
    - package.json exists with tauri dependencies
    - src-tauri/Cargo.toml exists with tauri crate
    - src-tauri/tauri.conf.json has correct productName "Opta"
  </verify>
  <done>Tauri v2 project scaffolded with React/TypeScript, configured with Opta branding</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and verify Rust toolchain</name>
  <files>package-lock.json, src-tauri/Cargo.lock</files>
  <action>
    1. Run `npm install` to install frontend dependencies
    2. Verify Rust toolchain: `rustc --version` (should be 1.70+)
    3. If Rust not installed, provide instructions but note this should be pre-installed
    4. Run `cd src-tauri && cargo check` to verify Rust compilation

    For cross-platform build support, ensure these Rust targets are available:
    - Current platform (already default)
    - Note: Additional targets added in Plan 01-03
  </action>
  <verify>
    - `npm install` completes without errors
    - `rustc --version` shows 1.70+
    - `cargo check` in src-tauri/ succeeds
  </verify>
  <done>All dependencies installed, Rust toolchain verified, project compiles</done>
</task>

<task type="auto">
  <name>Task 3: Verify development environment with test run</name>
  <files>None (verification only)</files>
  <action>
    1. Run `npm run tauri dev` to start development server
    2. Wait for app window to open (may take 1-2 minutes on first build)
    3. Verify:
       - Window opens with correct title "Opta - AI PC Optimizer"
       - React dev server hot-reload works (check console)
       - No console errors in dev tools
    4. Stop dev server after verification

    If build fails:
    - Check Rust compilation errors in terminal
    - Verify all Tauri CLI dependencies are installed
    - Check that WebView2 (Windows) or WebKit (macOS/Linux) is available
  </action>
  <verify>
    - `npm run tauri dev` launches app window
    - Window title shows "Opta - AI PC Optimizer"
    - No critical errors in console
  </verify>
  <done>Development environment fully functional with working hot-reload</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run tauri dev` launches without errors
- [ ] App window opens with correct branding
- [ ] React hot-reload works (edit App.tsx, see change)
- [ ] No TypeScript or Rust compilation errors
</verification>

<success_criteria>
- All tasks completed
- Tauri v2 project initialized with React/TypeScript
- Development environment working with hot-reload
- Project configured with Opta branding
- Ready for UI development in Plan 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
