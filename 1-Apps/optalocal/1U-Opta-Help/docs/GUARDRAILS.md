# Guardrails — Opta Help

1. Static export only — no API routes, no server actions
2. CSS variables only — never hex/rgb in component code
3. Framer Motion spring physics — no linear easings
4. Lucide React only for icons
5. All content as typed React components, not MDX
