{
  "name": "opta-code-desktop-universal",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "vite",
    "dev:native": "tauri dev",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit --pretty false",
    "parity:export": "node ../1D-Opta-CLI-TS/scripts/parity/export-cli-surface.mjs && node scripts/parity/check-cli-parity.mjs --export-only",
    "parity:check": "node scripts/parity/check-cli-parity.mjs",
    "test": "vitest --passWithNoTests",
    "test:run": "vitest run --passWithNoTests",
    "test:e2e": "node -e \"const { spawnSync } = require('node:child_process'); const command = process.platform === 'win32' ? 'npx.cmd' : 'npx'; const run = spawnSync(command, ['playwright', 'test'], { encoding: 'utf8' }); process.stdout.write(run.stdout || ''); process.stderr.write(run.stderr || ''); if (run.status === 0) process.exit(0); const output = (run.stdout || '') + (run.stderr || ''); const missingDeps = /Executable doesn't exist|Please run the following command to download new browsers|Host system is missing dependencies/i.test(output); if (missingDeps) { console.log('Skipping e2e: Playwright browser dependencies are unavailable.'); process.exit(0); } process.exit(run.status ?? 1);\"",
    "test:e2e:strict": "playwright test",
    "build": "tsc --noEmit && vite build",
    "check": "npm run check:desktop",
    "check:desktop": "npm run typecheck && npm run lint && npm run test:run && npm run parity:check && npm run build",
    "preview": "vite preview --port 5173",
    "deps:check": "npm outdated || true && npm audit",
    "deps:verify": "npm run typecheck && npm run test:run && npm run build && npm audit",
    "deps:upgrade:safe": "npm update && npm run deps:verify",
    "deps:upgrade:major": "npx npm-check-updates -u && npm install && npm run deps:verify"
  },
  "dependencies": {
    "@opta/daemon-client": "file:../1D-Opta-CLI-TS/packages/daemon-client",
    "@opta/protocol-shared": "file:../1D-Opta-CLI-TS/packages/protocol-shared",
    "@supabase/supabase-js": "^2.98.0",
    "@tauri-apps/plugin-opener": "^2.5.3",
    "clsx": "^2.1.1",
    "framer-motion": "12.34.4",
    "lucide-react": "^0.575.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-virtuoso": "^4.18.3",
    "tailwind-merge": "^3.5.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@tauri-apps/cli": "^2",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/node": "^22.19.13",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^5.1.4",
    "jsdom": "^27.4.0",
    "typescript": "^5.6.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
