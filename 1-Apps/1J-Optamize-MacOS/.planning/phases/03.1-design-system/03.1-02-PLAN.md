---
phase: 03.1-design-system
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified: [src/components/Layout.tsx, src/components/Sidebar.tsx, src/components/Sidebar.css, src/components/Layout.css, src/App.tsx, src/App.css, src/pages/Dashboard.tsx, src/pages/Dashboard.css, src/pages/Settings.tsx, src/pages/Settings.css, src/pages/Optimize.tsx, src/pages/Optimize.css]
autonomous: true
---

<objective>
Restyle navigation, layout, and page components using shadcn/ui and Tailwind.

Purpose: Transform the app shell from custom CSS to a cohesive futuristic design system.
Output: Polished sidebar, layout, and page structure with consistent styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan (design system setup):
@.planning/phases/03.1-design-system/03.1-01-PLAN.md

Source files to restyle:
@src/components/Layout.tsx
@src/components/Sidebar.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Sidebar with shadcn/ui components</name>
  <files>src/components/Sidebar.tsx, src/components/Sidebar.css</files>
  <action>
1. Rewrite Sidebar.tsx using Tailwind classes:
   - Fixed width sidebar (w-64) with dark background
   - Logo/brand at top with subtle glow effect
   - Navigation items using shadcn Button variant="ghost"
   - Active state with primary color highlight and left border
   - Icons for each nav item (Dashboard, Optimize, Settings)
   - Hover states with subtle background change

2. Design approach:
   - Clean, minimal - no heavy borders or shadows
   - Active item: bg-primary/10, border-l-2 border-primary, text-primary
   - Hover: bg-muted/50
   - Icons: Use simple Unicode or inline SVG (no icon library needed)

3. Delete Sidebar.css - all styles in Tailwind now

Keep it powerful but simple - the sidebar should feel premium, not busy.
  </action>
  <verify>npm run dev (sidebar renders with new styling)</verify>
  <done>Sidebar restyled with Tailwind, feels futuristic and clean</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Layout and App shell</name>
  <files>src/components/Layout.tsx, src/components/Layout.css, src/App.tsx, src/App.css</files>
  <action>
1. Rewrite Layout.tsx:
   - Flex container: sidebar + main content
   - Main content area with proper padding and max-width
   - Subtle top border or separator between sections
   - Scrollable main area with custom scrollbar styling

2. Rewrite App.tsx:
   - Apply dark class to root for Tailwind dark mode
   - Clean structure with Layout wrapper
   - Remove any inline styles

3. Update App.css and Layout.css:
   - Minimal/delete - move everything to Tailwind
   - Keep only custom scrollbar styles if needed
   - Add subtle background pattern or gradient (optional, keep it subtle)

4. Overall feel:
   - Dark, immersive, like a gaming cockpit
   - Content should "float" on the dark background
   - Subtle depth with shadows, not heavy 3D effects
  </action>
  <verify>npm run dev (full layout renders correctly)</verify>
  <done>Layout and App shell restyled with cohesive dark theme</done>
</task>

<task type="auto">
  <name>Task 3: Restyle page containers (Dashboard, Settings, Optimize)</name>
  <files>src/pages/Dashboard.tsx, src/pages/Dashboard.css, src/pages/Settings.tsx, src/pages/Settings.css, src/pages/Optimize.tsx, src/pages/Optimize.css</files>
  <action>
1. Dashboard.tsx:
   - Page title with subtle glow effect
   - Section headings styled consistently
   - Grid layout for telemetry cards using Tailwind grid
   - Proper spacing between sections (space-y-6)
   - Keep component imports (TelemetryCard, ProcessList, StealthMode)

2. Settings.tsx:
   - Clean settings layout with shadcn Card components
   - Section headers
   - Placeholder content for now (will be populated later)

3. Optimize.tsx:
   - Page structure ready for game optimization features
   - Hero section with call-to-action
   - Placeholder for game list (Phase 7)

4. Delete/minimize CSS files:
   - Dashboard.css, Settings.css, Optimize.css - move to Tailwind
   - Only keep truly custom animations if needed

Consistency is key - all pages should feel like the same app.
  </action>
  <verify>npm run dev (all pages render with consistent styling)</verify>
  <done>All page containers restyled with consistent futuristic theme</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Sidebar navigation works and looks polished
- [ ] Layout has proper dark theme throughout
- [ ] All three pages (Dashboard, Settings, Optimize) render correctly
- [ ] Navigation between pages works
- [ ] No CSS conflicts or visual bugs
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- App shell feels futuristic, powerful, and simple
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system/03.1-02-SUMMARY.md`
</output>
