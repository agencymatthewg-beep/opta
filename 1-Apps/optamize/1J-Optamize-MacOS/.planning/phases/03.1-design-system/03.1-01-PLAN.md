---
phase: 03.1-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tailwind.config.js, postcss.config.js, src/index.css, src/lib/utils.ts, components.json, tsconfig.json]
autonomous: true
---

<objective>
Install and configure shadcn/ui with Tailwind CSS for a futuristic, powerful, simple design system.

Purpose: Establish the design foundation that all components will use - dark theme, clean typography, subtle animations.
Output: Working shadcn/ui setup with Tailwind, ready for component installation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current package.json:
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind CSS and configure for Vite</name>
  <files>package.json, tailwind.config.js, postcss.config.js, src/index.css</files>
  <action>
1. Install Tailwind CSS and dependencies:
   ```
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

2. Configure tailwind.config.js:
   - content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
   - darkMode: "class"
   - Add custom colors for futuristic theme:
     - primary: neon cyan (#00f0ff)
     - accent: electric purple (#a855f7)
     - success: neon green (#00ff88)
     - warning: amber (#f59e0b)
     - danger: red (#ef4444)
     - background: near-black (#0a0a0f)
     - card: dark gray (#111118)
     - border: subtle gray (#1f1f2e)

3. Update src/index.css:
   - Add Tailwind directives (@tailwind base/components/utilities)
   - Set dark mode defaults (bg-background, text-white)
   - Add custom CSS variables for the theme
   - Remove old global styles that conflict

IMPORTANT: Keep the CSS variables approach for easy theming later.
  </action>
  <verify>npm run build (should compile without Tailwind errors)</verify>
  <done>Tailwind CSS configured with futuristic dark theme colors</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and install core components</name>
  <files>components.json, src/lib/utils.ts, tsconfig.json</files>
  <action>
1. Initialize shadcn/ui:
   ```
   npx shadcn@latest init
   ```
   - Style: New York (cleaner, more minimal)
   - Base color: Slate
   - CSS variables: Yes
   - Path aliases: Configure @/components, @/lib

2. Update tsconfig.json for path aliases:
   ```json
   "compilerOptions": {
     "baseUrl": ".",
     "paths": {
       "@/*": ["./src/*"]
     }
   }
   ```

3. Install core shadcn/ui components needed for restyle:
   ```
   npx shadcn@latest add button card badge dialog progress table scroll-area separator tooltip
   ```

4. Create src/lib/utils.ts with cn() helper (shadcn standard)

AVOID: Don't install all components - only what we need. Keep bundle small.
  </action>
  <verify>ls src/components/ui/ (should show installed shadcn components)</verify>
  <done>shadcn/ui initialized with core components installed</done>
</task>

<task type="auto">
  <name>Task 3: Configure futuristic theme overrides</name>
  <files>src/index.css, tailwind.config.js</files>
  <action>
1. Update src/index.css with futuristic theme:
   - Override shadcn CSS variables for dark mode
   - Add glow utilities (.glow-sm, .glow-md, .glow-lg)
   - Add subtle animation keyframes (pulse-glow, fade-in)
   - Set default font to system UI with fallbacks

2. Theme colors (override shadcn defaults):
   ```css
   :root {
     --background: 240 10% 4%;      /* #0a0a0f - near black */
     --foreground: 0 0% 98%;        /* white text */
     --card: 240 10% 7%;            /* #111118 - dark card */
     --card-foreground: 0 0% 98%;
     --primary: 185 100% 47%;       /* #00f0ff - neon cyan */
     --primary-foreground: 240 10% 4%;
     --secondary: 240 10% 12%;
     --accent: 270 91% 65%;         /* #a855f7 - electric purple */
     --muted: 240 10% 15%;
     --border: 240 10% 15%;
   }
   ```

3. Add utility classes for futuristic effects:
   - .glass - frosted glass effect
   - .glow-primary - cyan glow
   - .glow-success - green glow (for Stealth Mode)
   - .animate-pulse-glow - subtle pulsing glow

Keep it minimal - powerful simplicity, not overwhelming effects.
  </action>
  <verify>npm run dev (app should load with new dark theme)</verify>
  <done>Futuristic theme configured with glow effects and custom colors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` shows app with dark background
- [ ] src/components/ui/ contains shadcn components
- [ ] Tailwind classes work in existing components
- [ ] CSS variables are properly set for dark mode
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Design system foundation ready for component restyle
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system/03.1-01-SUMMARY.md`
</output>
