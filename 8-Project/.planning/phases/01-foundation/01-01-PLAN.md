---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/AICompare/package.json, apps/web/AICompare/next.config.ts, apps/web/AICompare/tsconfig.json, apps/web/AICompare/src/app/layout.tsx, apps/web/AICompare/src/app/page.tsx]
autonomous: true
---

<objective>
Scaffold the Next.js 15 project with TypeScript, Tailwind CSS 4, and App Router, then install design system dependencies.

Purpose: Establish the project foundation that all subsequent phases build upon. This creates a separate repository from the Opta monorepo as specified in PROJECT.md constraints.

Output: A working Next.js 15 project in `apps/web/AICompare/` directory with all core and design system dependencies installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with App Router</name>
  <files>apps/web/AICompare/</files>
  <action>
  Run `npx create-next-app@latest aicomp` with these options:
  - TypeScript: Yes
  - ESLint: Yes
  - Tailwind CSS: Yes
  - src/ directory: Yes
  - App Router: Yes
  - Turbopack: Yes (for dev)
  - Import alias: @/* (default)

  Accept defaults for everything else. The project should be created in `apps/web/AICompare/` directory at repo root (separate from existing Opta apps).

  AVOID: Creating inside an existing app directory. This is a NEW standalone project.
  </action>
  <verify>cd apps/web/AICompare && npm run dev starts without errors (then Ctrl+C)</verify>
  <done>Next.js 15 project created with TypeScript, Tailwind, App Router enabled</done>
</task>

<task type="auto">
  <name>Task 2: Set up directory structure</name>
  <files>apps/web/AICompare/src/components/, apps/web/AICompare/src/lib/, apps/web/AICompare/src/styles/</files>
  <action>
  Create the organized directory structure from research:

  ```
  apps/web/AICompare/src/
  ├── app/           # Already exists from create-next-app
  ├── components/
  │   ├── ui/        # Primitive components (Button, Card, Badge)
  │   ├── layout/    # Layout components (Sidebar, Header)
  │   └── features/  # Feature-specific (will be populated later)
  ├── lib/           # Utilities, helpers
  └── styles/        # Global styles (globals.css already exists)
  ```

  Create placeholder `.gitkeep` files in empty directories to ensure they're tracked.

  AVOID: Putting all components in flat `components/` - use subdirectories for organization.
  </action>
  <verify>ls -la apps/web/AICompare/src/components/ shows ui/, layout/, features/ directories</verify>
  <done>Directory structure created matching 2025 best practices</done>
</task>

<task type="auto">
  <name>Task 3: Install design system dependencies</name>
  <files>apps/web/AICompare/package.json</files>
  <action>
  Install dependencies from RESEARCH.md recommendations:

  ```bash
  cd apps/web/AICompare
  npm install @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-tooltip @radix-ui/react-select
  npm install class-variance-authority tailwind-merge
  npm install framer-motion
  npm install lucide-react
  ```

  These provide:
  - Radix UI: Accessible primitives (Dialog, Dropdown, Tooltip, Select)
  - CVA + tailwind-merge: Type-safe component variants
  - Framer Motion: Animations
  - Lucide: Icons

  AVOID: Installing unnecessary dependencies. Only install what's specified above.
  </action>
  <verify>cd apps/web/AICompare && npm ls @radix-ui/react-dialog class-variance-authority framer-motion lucide-react shows all installed</verify>
  <done>All design system dependencies installed and visible in package.json</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/web/AICompare && npm run build` succeeds without errors
- [ ] `cd apps/web/AICompare && npm run dev` starts development server
- [ ] Directory structure exists: `src/components/{ui,layout,features}/`, `src/lib/`
- [ ] All design system packages in package.json dependencies
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Project builds and runs without errors
- Clean foundation ready for design system implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
