---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [apps/ios/ClawdbotMobile/ClawdbotMobile.xcodeproj, apps/ios/ClawdbotMobile/ClawdbotMobile/ClawdbotMobileApp.swift, apps/desktop/ClawdbotDesktop/ClawdbotDesktop.xcodeproj, apps/desktop/ClawdbotDesktop/ClawdbotDesktop/ClawdbotDesktopApp.swift]
autonomous: false
---

<objective>
Scaffold the iOS and macOS app targets that will become the Clawdbot native apps.

Purpose: Create minimal working app shells that import ClawdbotKit and display a placeholder UI, establishing the project structure for all future development.
Output: Two Xcode projects (ClawdbotMobile, ClawdbotDesktop) that compile and run with placeholder content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing app patterns to follow:
@apps/ios/opta/Opta Scan/Opta_ScanApp.swift
@apps/desktop/opta-mini/OptaMini/OptaMiniApp.swift

# Package to import:
@apps/ios/ClawdbotKit/Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create iOS app scaffold (ClawdbotMobile)</name>
  <files>apps/ios/ClawdbotMobile/ClawdbotMobile/ClawdbotMobileApp.swift, apps/ios/ClawdbotMobile/ClawdbotMobile/ContentView.swift, apps/ios/ClawdbotMobile/ClawdbotMobile/Info.plist</files>
  <action>
Create iOS app at `apps/ios/ClawdbotMobile/`:

1. Create Xcode project structure:
   ```
   ClawdbotMobile/
   ├── ClawdbotMobile.xcodeproj/
   ├── ClawdbotMobile/
   │   ├── ClawdbotMobileApp.swift    # @main entry point
   │   ├── ContentView.swift          # Placeholder UI
   │   ├── Assets.xcassets/           # App icon, colors
   │   └── Info.plist
   └── ClawdbotMobileTests/
   ```

2. ClawdbotMobileApp.swift:
   - Import SwiftUI and ClawdbotKit
   - @main App struct
   - Single WindowGroup with ContentView

3. ContentView.swift:
   - Import ClawdbotKit
   - Display "Clawdbot" title using ClawdbotColors (once Plan 02 completes)
   - For now, use placeholder colors if design system not yet available
   - Simple VStack with app name and version placeholder

4. Configure project settings:
   - Deployment target: iOS 17.0
   - Swift language version: 5.9
   - Add ClawdbotKit as local package dependency (path: ../ClawdbotKit)

Use `xcodebuild` CLI to create project or create files manually following existing opta structure.
  </action>
  <verify>cd apps/ios/ClawdbotMobile && xcodebuild -scheme ClawdbotMobile -destination 'platform=iOS Simulator,name=iPhone 16' build</verify>
  <done>iOS app compiles and can run in Simulator</done>
</task>

<task type="auto">
  <name>Task 2: Create macOS app scaffold (ClawdbotDesktop)</name>
  <files>apps/desktop/ClawdbotDesktop/ClawdbotDesktop/ClawdbotDesktopApp.swift, apps/desktop/ClawdbotDesktop/ClawdbotDesktop/ContentView.swift, apps/desktop/ClawdbotDesktop/ClawdbotDesktop/Info.plist</files>
  <action>
Create macOS app at `apps/desktop/ClawdbotDesktop/`:

1. Create Xcode project structure:
   ```
   ClawdbotDesktop/
   ├── ClawdbotDesktop.xcodeproj/
   ├── ClawdbotDesktop/
   │   ├── ClawdbotDesktopApp.swift   # @main entry point
   │   ├── ContentView.swift          # Placeholder UI
   │   ├── Assets.xcassets/
   │   └── Info.plist
   │   └── ClawdbotDesktop.entitlements
   └── ClawdbotDesktopTests/
   ```

2. ClawdbotDesktopApp.swift:
   - Import SwiftUI and ClawdbotKit
   - @main App struct
   - WindowGroup with ContentView
   - Set window size constraints (min 400x600)

3. ContentView.swift:
   - Import ClawdbotKit
   - Placeholder chat-style layout
   - Use design system colors when available

4. Configure project settings:
   - Deployment target: macOS 14.0
   - Swift language version: 5.9
   - Add ClawdbotKit as local package dependency (path: ../../ios/ClawdbotKit)
   - Enable App Sandbox in entitlements
   - Enable outgoing network connections (for WebSocket in Phase 2)

Follow opta-mini patterns for macOS-specific settings.
  </action>
  <verify>cd apps/desktop/ClawdbotDesktop && xcodebuild -scheme ClawdbotDesktop build</verify>
  <done>macOS app compiles and can run</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>iOS and macOS app scaffolds with ClawdbotKit integration</what-built>
  <how-to-verify>
    1. Open apps/ios/ClawdbotMobile/ClawdbotMobile.xcodeproj in Xcode
    2. Build and run on iOS Simulator (iPhone 16)
    3. Verify app launches showing "Clawdbot" placeholder
    4. Open apps/desktop/ClawdbotDesktop/ClawdbotDesktop.xcodeproj in Xcode
    5. Build and run on macOS
    6. Verify app launches showing placeholder window
    7. Both apps should import ClawdbotKit without errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ClawdbotMobile.xcodeproj exists and compiles
- [ ] ClawdbotDesktop.xcodeproj exists and compiles
- [ ] Both apps successfully import ClawdbotKit
- [ ] iOS app runs in Simulator
- [ ] macOS app runs on desktop
- [ ] Human verification passed
</verification>

<success_criteria>
- All tasks completed
- Both apps compile without errors
- ClawdbotKit package dependency resolved in both projects
- Apps display placeholder UI
- Human verified apps launch correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
