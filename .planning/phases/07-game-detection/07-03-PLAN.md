---
phase: 07-game-detection
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/pages/Games.tsx, src/components/GameCard.tsx, src/components/GameList.tsx, src/App.tsx]
autonomous: true
---

<objective>
Create game profile management UI to browse detected games and view optimizations.

Purpose: Allow users to see their games and available optimization settings.
Output: Games page with game cards, optimization tips, and settings preview.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior plan (game detection):
@.planning/phases/07-game-detection/07-01-PLAN.md

Design system:
@.planning/phases/03.1-design-system/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameCard and GameList components</name>
  <files>src/components/GameCard.tsx, src/components/GameList.tsx</files>
  <action>
1. Create `GameCard.tsx`:
   - Display single game with launcher badge
   - Props: game (DetectedGame), onClick, optimizationAvailable
   - Show: name, launcher icon, install size
   - Badge if optimization available: "Optimizations Available"
   - Futuristic card style with hover glow

   ```typescript
   interface GameCardProps {
     game: DetectedGame;
     hasOptimization: boolean;
     onClick: () => void;
   }

   // Launcher icons/colors:
   // Steam: blue
   // Epic: dark
   // GOG: purple
   ```

2. Create `GameList.tsx`:
   - Grid of GameCard components
   - Props: games, onGameSelect
   - Filter by launcher (tabs or dropdown)
   - Search by name
   - Sort options: name, launcher, recently played
   - Empty state if no games found

3. Styling (shadcn + Tailwind):
   - Card grid: responsive 2-4 columns
   - Launcher filter pills with icons
   - Search input with icon
   - Subtle animations on card hover

Follow existing design patterns from TelemetryCard.
  </action>
  <verify>npm run build</verify>
  <done>GameCard and GameList components render games</done>
</task>

<task type="auto">
  <name>Task 2: Create Games page</name>
  <files>src/pages/Games.tsx, src/App.tsx</files>
  <action>
1. Create `Games.tsx` page:
   - Header: "Games" with game count
   - Subheader: "Detected games across your launchers"
   - GameList with filtering
   - Selected game detail panel (side or modal)

2. Selected game detail:
   - Large game name
   - Launcher and install path
   - Optimization section:
     - If known: show tips and settings preview
     - If unknown: "AI can generate recommendations"
   - "Optimize" button (disabled until Phase 8)

3. Layout structure:
   ```typescript
   // Option A: Split view (list left, detail right)
   // Option B: Modal on card click
   // Choose based on screen space - Option A preferred

   const [selectedGame, setSelectedGame] = useState<DetectedGame | null>(null);
   ```

4. Add to App.tsx routing:
   - Route: "/games"
   - Add to Sidebar navigation

5. Empty states:
   - No launchers detected: "Install Steam, Epic, or GOG"
   - No games: "No games found in your libraries"
   - Loading: skeleton cards

Match the futuristic design of Dashboard.
  </action>
  <verify>npm run dev (Games page shows detected games)</verify>
  <done>Games page displays detected games with filtering</done>
</task>

<task type="auto">
  <name>Task 3: Add game optimization preview</name>
  <files>src/components/GameOptimizationPreview.tsx, src/pages/Games.tsx</files>
  <action>
1. Create `GameOptimizationPreview.tsx`:
   - Show optimization settings in readable format
   - Props: optimization (GameOptimization | null), loading
   - Sections: Graphics, Launch Options, Tips
   - Source badge: "Community Verified" or "AI Generated"

   ```typescript
   interface GameOptimizationPreviewProps {
     optimization: GameOptimization | null;
     loading: boolean;
   }

   // Display settings as key-value pairs
   // Graphics: shadows=low, vsync=off, etc.
   // Tips: numbered list with icons
   ```

2. Trust indicators:
   - Community verified: green check, "Tested by community"
   - AI generated: blue AI icon, "AI suggestion - verify before applying"

3. Integration with Games page:
   - Load optimization when game selected
   - Show preview in detail panel
   - "Apply Optimization" button (disabled, Phase 8)
   - "Ask AI for Custom Settings" button

4. No optimization available state:
   - "No community settings for this game"
   - "Generate AI recommendations?" button
   - Disclaimer about AI suggestions

Keep settings read-only - actual application is Phase 8.
  </action>
  <verify>npm run build</verify>
  <done>Game optimization preview shows settings and tips</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] GameCard renders with launcher badge
- [ ] GameList filters by launcher
- [ ] Games page accessible from sidebar
- [ ] Selected game shows optimization preview
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- UI matches futuristic design system
- Optimization preview clearly shows source
</success_criteria>

<output>
After completion, create `.planning/phases/07-game-detection/07-03-SUMMARY.md`
</output>
