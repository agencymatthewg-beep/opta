---
phase: 02-hardware-telemetry
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/components/TelemetryCard.tsx, src/components/TelemetryCard.css, src/components/CpuMeter.tsx, src/components/MemoryMeter.tsx, src/components/GpuMeter.tsx, src/pages/Dashboard.tsx, src/pages/Dashboard.css]
autonomous: true
---

<objective>
Create real-time telemetry dashboard widgets showing CPU, RAM, disk, and GPU stats.

Purpose: Display live system metrics in the Dashboard page with gaming-aesthetic styling.
Output: Dashboard with animated meters and real-time updates every 2 seconds.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hardware-telemetry/02-01-SUMMARY.md
@src/pages/Dashboard.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create telemetry widget components</name>
  <files>src/components/TelemetryCard.tsx, src/components/TelemetryCard.css, src/components/CpuMeter.tsx, src/components/MemoryMeter.tsx, src/components/GpuMeter.tsx</files>
  <action>
    Create reusable telemetry display components:

    1. TelemetryCard.tsx - Base card wrapper:
       - Title prop (e.g., "CPU", "Memory")
       - Icon prop (emoji or SVG)
       - Children slot for meter content
       - Gaming aesthetic: dark bg (#1a1a1a), subtle border, glow on hover
       - Compact size for dashboard grid

    2. CpuMeter.tsx:
       - Circular progress ring showing CPU % (0-100)
       - Neon green (#00ff88) for the progress arc
       - Center text: "XX%" large, "CPU" small below
       - Animate the arc smoothly on value changes
       - Below ring: "X cores / Y threads"

    3. MemoryMeter.tsx:
       - Horizontal bar showing used/total
       - Gradient from green (low) to yellow to red (high usage)
       - Text: "X.X / Y.Y GB" and "XX%"
       - Subtle animation on value changes

    4. GpuMeter.tsx:
       - Similar to CPU meter but for GPU
       - Show GPU name at bottom
       - If GPU not available: Show "No GPU detected" with muted styling
       - Optional: temperature badge if available

    Use CSS animations for smooth transitions. Follow existing CSS variable patterns from Phase 1.
  </action>
  <verify>
    - All components render without errors
    - TypeScript types are correct
    - Styling matches gaming aesthetic from Phase 1
  </verify>
  <done>Telemetry widget components created with gaming aesthetic</done>
</task>

<task type="auto">
  <name>Task 2: Implement real-time polling/updates</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Wire up telemetry hook to Dashboard and display live data:

    1. Import useTelemetry hook and telemetry components:
       ```typescript
       import { useTelemetry } from '../hooks/useTelemetry';
       import { CpuMeter } from '../components/CpuMeter';
       import { MemoryMeter } from '../components/MemoryMeter';
       import { GpuMeter } from '../components/GpuMeter';
       import { TelemetryCard } from '../components/TelemetryCard';
       ```

    2. Use hook with 2-second polling:
       ```typescript
       const { telemetry, loading, error } = useTelemetry(2000);
       ```

    3. Handle states:
       - Loading: Show skeleton/placeholder cards
       - Error: Show error message with retry button
       - Success: Display telemetry meters

    4. Layout: CSS Grid with 2x2 or responsive layout:
       - CPU meter (top-left)
       - Memory meter (top-right)
       - GPU meter (bottom-left)
       - Disk meter (bottom-right)

    5. Add "Last updated: X seconds ago" footer.

    NOTE: If Plan 02-02 isn't complete yet, use mock data for development:
    ```typescript
    const mockTelemetry = {
      cpu: { percent: 45, cores: 8, threads: 16 },
      memory: { total_gb: 32, used_gb: 18.5, percent: 58 },
      // ...
    };
    ```
  </action>
  <verify>
    - Dashboard shows telemetry meters
    - Values update every 2 seconds
    - Loading and error states work
  </verify>
  <done>Dashboard displays real-time telemetry with 2-second polling</done>
</task>

<task type="auto">
  <name>Task 3: Style with gaming aesthetic matching Phase 1</name>
  <files>src/components/TelemetryCard.css, src/pages/Dashboard.css</files>
  <action>
    Polish the styling to match the gaming aesthetic from Phase 1:

    1. TelemetryCard styling:
       - Background: var(--bg-card) (#1a1a1a)
       - Border: 1px solid var(--border) (#333)
       - Border-radius: 12px
       - Padding: 16px
       - Hover: subtle glow with var(--accent-glow)
       - Transition: all 0.2s ease

    2. Meter animations:
       - CSS transitions for value changes (0.5s ease-out)
       - Pulse animation on high values (>90%)
       - Subtle glow on accent elements

    3. Progress rings/bars:
       - Use SVG for circular meters (better animation)
       - Stroke: var(--accent) for progress
       - Background stroke: var(--border)
       - Smooth dashoffset animations

    4. Color coding for values:
       - 0-60%: var(--accent) green (#00ff88)
       - 60-85%: #ffaa00 (warning yellow)
       - 85-100%: #ff4444 (danger red)

    5. Typography:
       - Large percentage: 32px bold
       - Labels: 12px uppercase, var(--text-secondary)
       - Use system-ui font from Phase 1

    6. Responsive: Stack cards on narrow screens.
  </action>
  <verify>
    - Visual consistency with Phase 1 sidebar/pages
    - Smooth animations (no jank)
    - Color coding works for different values
    - Responsive layout works
  </verify>
  <done>Telemetry dashboard styled with polished gaming aesthetic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All telemetry components render correctly
- [ ] Real-time updates working (values change)
- [ ] Gaming aesthetic consistent with Phase 1
- [ ] Loading/error states handled gracefully
- [ ] `npm run tauri dev` shows working dashboard
</verification>

<success_criteria>
- All tasks completed
- Dashboard shows live CPU, RAM, disk, GPU stats
- Updates every 2 seconds smoothly
- Matches gaming aesthetic from Phase 1
- Phase 2 complete - ready for Phase 3 (Process Management)
</success_criteria>

<output>
After completion, create `.planning/phases/02-hardware-telemetry/02-03-SUMMARY.md`
</output>
